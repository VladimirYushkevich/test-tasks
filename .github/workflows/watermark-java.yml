name: watermark-java

on:
  push:
    branches:
      - master
    paths:
      - 'services/watermark-java/**'

jobs:
  build:
    name: Build & Publish
    runs-on: ubuntu-latest
    steps:
    - name: Git checkout
    - uses: actions/checkout@v1